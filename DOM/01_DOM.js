const ullist = document.querySelector('ul');
 ullist.querySelectorAll('li').forEach((i)=>{
    i.style.backgroundColor = "green"
 })
// ulist.forEach((i)=>{
//     i.style.backgroundColor = 'yellow';
// })

const para = document.querySelector('#heading1');
para.style.backgroundColor ="orange";
para.innerText = "Tusar pal"

// HTML collection convert in array(class attribute )
// Array.from() ---------> use to convert HTMLcollection to array


document.getElementById('heading1').style.color = 'blue'

